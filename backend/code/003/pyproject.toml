[project]
name = "dplr-cs-ssm-mve"
version = "0.1.0"
description = "MVE for DPLR Column-Sparse SSM (Proposal 003)"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.0.0",
    "numpy>=1.24.0",
    "tqdm>=4.65.0",
    "pyyaml>=6.0",
]
